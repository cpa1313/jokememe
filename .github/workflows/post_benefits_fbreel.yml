name: 🎬 Benefits Reel Poster

on:
  schedule:
    # Deliberately non-round minute/hour values instead of e.g. "0 10 * * *" —
    # this alone breaks the "posted at the exact same clean timestamp every
    # day" pattern without burning runner minutes on an in-job sleep.
    - cron: "13 1 * * *"   # ~09:13 PHT
    - cron: "47 5 * * *"   # ~13:47 PHT
    - cron: "22 10 * * *"  # ~18:22 PHT
  workflow_dispatch:

permissions:
  contents: write

jobs:
  post-reel:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install FFmpeg
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y ffmpeg
      - name: Install dependencies
        run: pip install requests Pillow
      - name: Prepare output folder
        run: mkdir -p output
      - name: Generate and post benefits reel
        env:
          FB_ACCESS_TOKEN: ${{ secrets.FB_ACCESS_TOKEN }}
          FB_PAGE_ID: ${{ secrets.FB_PAGE_ID }}
        run: python viralvideo_benefits_maker.py
      - name: Upload rendered video
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: benefits-reel-${{ github.run_number }}
          path: output/reel.mp4
          retention-days: 7
      - name: Save posting progress
        if: always()
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add benefits_progress.json
          git diff --staged --quiet || git commit -m "chore: update benefits progress [skip ci]"
          git pull --rebase
          git push
